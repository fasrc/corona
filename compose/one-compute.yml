one-compute:
    image: dockernebula/base:latest
    name: one-compute

libvirt:
    image: dockernebula/libvirt:latest
    name: libvirt
    net: host
    pid: host
    privileged: true
    restart: always
    volumes:
     - /run:/run
     - /sys/fs/cgroup:/sys/fs/cgroup
     - /dev:/dev
